<header class="">
      <h1 class="px-4 mt-4 mb-4 text-4xl font-black text-center"><a href="{{ '/' | url }}">{{ metadata.title }}</a></h1>
      <div id="nav" class="flex text-center">
      <ul class="float-left list-none p">
        {%- for nav in collections.nav -%}
        <li class="inline-flex {% if nav.url == page.url %} font-bold underline{% endif %}"><a href="{{ nav.url | url }}" class="">{{ nav.data.navtitle }}</a></li>
        {%- endfor -%}
      </ul>
      </div>
    </header>